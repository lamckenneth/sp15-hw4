<h1>Create New Dog</h1>
  <%= form_for @dog do |f| %> <!-- Create form for user object -->
    Name: <%= f.text_field :name %> <!-- Create a text field that sends its value under the key name-->
    Age: <%= f.number_field :age %><!-- Create a integer field that sends its value under the key age -->
    <%= f.submit %> <!-- Create submit button-->
  <% end %>

